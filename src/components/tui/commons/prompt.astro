---
interface Props {
  /** comando que se muestra tras el `$` */
  cmd: string;
  class?: string;
}

const { cmd, class: className } = Astro.props;
---

<p class:list={["text-[0.85rem] text-dim", className ?? "mb-[0.6rem]"]}>
  <span class="text-accent">$</span>{" "}{cmd}
</p>
