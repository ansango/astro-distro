---
import { fetchLogo } from "../../lib/fastfetch";

const distro = "debian";
const [full, small] = await fetchLogo(distro);
---

<pre>
    <span class="md:hidden">
{small}
    </span>
    <span class="hidden md:block">
{full}
    </span>
</pre>
